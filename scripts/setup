#!/usr/bin/env bash
set -euo pipefail

uv sync --locked --group dev

if command -v npm >/dev/null 2>&1; then
  npm ci
else
  printf 'npm is required for markdown, prettier, and pyright tooling.\n' >&2
  exit 1
fi
