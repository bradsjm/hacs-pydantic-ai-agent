#!/usr/bin/env bash
set -euo pipefail

uv sync --group dev

if command -v pnpm >/dev/null 2>&1; then
  pnpm install --frozen-lockfile
else
  printf 'pnpm is required for markdown and prettier tooling.\n' >&2
  exit 1
fi
