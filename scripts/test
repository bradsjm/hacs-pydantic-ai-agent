#!/usr/bin/env bash
set -euo pipefail

uv run pytest \
  --timeout=10 \
  -n "${PYTEST_WORKERS:-auto}" \
  --dist=loadfile \
  -m "not provider_integration" \
  tests/components/pydantic_ai_agent \
  "$@"
